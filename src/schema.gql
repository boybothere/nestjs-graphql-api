# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Achievement {
  id: ID!
  title: String!
  description: String!
  points: Int!
  difficulty: Difficulty!
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
}

type Game {
  id: ID!
  name: String!
  genre: String
  achievement(skip: Int!, take: Int!, difficulty: Difficulty): [Achievement!]!
}

type Query {
  games(take: Int!, skip: Int!): [Game!]!
  game(id: Int!): Game!
}